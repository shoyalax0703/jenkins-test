pipeline {
    agent any

    stages {
        stage('Test kubectl with Plugin') {
            steps {
                withKubeConfig(credentialsId: 'kind') {
                    sh 'kubectl get nodes'
                }
            }
        }
    }
}